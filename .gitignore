# ============================================
# VoiceGuard AI - Git Ignore File
# ============================================

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
*.egg

# Virtual environments
venv/
ENV/
env/
.venv/

# IDE
.idea/
.vscode/
*.swp
*.swo
*.swn

# OS
.DS_Store
Thumbs.db

# Git
.git/

# Logs
*.log
logs/*.log

# Temporary files
temp_uploads/
*.tmp
*.temp

# Test files
tests/
test_*.py
.pytest_cache/
.coverage
htmlcov/

# ============================================
# IMPORTANT: Allow .gitkeep files
# ============================================
# Don't ignore .gitkeep files (they preserve folder structure)
!.gitkeep
!**/.gitkeep

# ============================================
# Exclude large user data (but keep .gitkeep)
# ============================================

# Training data files (but keep folder structure)
data/*.wav
data/*.mp3
data/*.flac
data/*.m4a
data/human/*.wav
data/human/*.mp3
data/fake/*.wav
data/fake/*.mp3

# Trained models (large files - users train their own)
models/*.bin
models/*.safetensors
models/*.pt
models/*.pth
models/voice_guard_model/*.bin
models/voice_guard_model/*.safetensors

# User recordings (privacy - don't commit actual recordings)
recordings/*.wav
recordings/*.mp3

# Hugging Face cache (downloaded at runtime)
hf_cache/
.cache/
.huggingface/

# Docker (keep the files, exclude build artifacts)
# Keep: Dockerfile, docker-compose.yml, .dockerignore

# Node modules
node_modules/

# Jupyter
.ipynb_checkpoints/
*.ipynb
